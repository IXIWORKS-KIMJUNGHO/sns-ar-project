import 'package:flutter/material.dart';

class ModelViewerWebWidget extends StatelessWidget {
  const ModelViewerWebWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Model Viewer is only supported on Web'),
    );
  }
}